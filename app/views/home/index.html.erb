<div class="flex justify-between mb-5">
	<h1 class="text-2xl">הניקוד שלי</h1>
	<h1 class="text-2xl ml-32"><%= @my_score %></h1>
</div>
<%= @a %>
<h1 class="text-xl">המשימות שלי</h1>
<div class="rounded-lg bg-gray-100 shadow px-4 py-4">
	<% @my_tasks.each do |task| %>
		<%= render "task", { task: task, status: "in_progress"} %>
	<% end %>
</div>
<br>
<%= @group_score %>
<h1 class="text-xl">משימות תנועתיות</h1>
<div class="rounded-lg bg-gray-100 shadow px-4 py-4">
	<% @random_tasks.each do |task| %>
		<%= render "task", { task: task, status: "random"} %>
	<% end %>
</div>
<br>
<h1 class="text-xl">משימות שסיימתי</h1>
<div class="rounded-lg bg-gray-100 shadow px-4 py-4">
	<div class="w-full py-1 shadow my-2 rounded-lg  bg-gray-200 text-gray-700">
		<div class="w-full flex justify-between">
			<span class="mx-3 text-lg aw-1/3 line-through">משימה לא אמיתית</span>
			<span class="mx-3 w-1/3 flex justify-end"><i class="gg-time"></i></span>
		</div>
		<span class="mx-3 aw-1/3">2 נקודות</span>
	</div>
	<div class="w-full py-1 shadow my-2 rounded-lg  bg-gray-200 text-gray-700">
		<div class="w-full flex justify-between">
			<span class="mx-3 text-lg aw-1/3 line-through">משימה לא אמיתית</span>
			<!-- <span class="mx-3 w-1/3 flex justify-end"><i class="gg-check-o"></i></span> -->
		</div>
		<span class="mx-3 aw-1/3">2 נקודות</span>
	</div>
	<% @done_tasks.each do |task| %>
		<%= render "task", { task: task, status: "done"} %>
	<% end %>
	<% @approved_tasks.each do |task| %>
		<%= render "task", { task: task, status: "approved"} %>
	<% end %>
</div>


<br><br><br><br><br>
<div class="">
  המשימות שלי
</div>

<div class="">
  הניקוד שלי ביחס לאחרים
  <br>Leaderboard
</div>

<div class="">
  החניכים שלי, אם אני מדריך
  <br>משימות שאני צריך לאשר
</div>

<div class="">
  המדריכים שלי, אם אני רכז קן
</div>

<div class="">
  רכזי הקנים שלי, אם אני רכז אזור
</div>

<div class="">
  רכזי האזור שלי, אם אני רכז תנועה
</div>

<div class="">
  התנועות שלי, אם אני אדמין
</div>
